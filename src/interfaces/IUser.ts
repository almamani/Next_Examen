export interface IUser {
  login: boolean;
  name: string;
  email: string;
}

/* export interface IUser {
  login: boolean;
  user: UserData;
}

interface UserData {
  id: number;
  name: string;
  email: string;
  password: string;
}
 */
